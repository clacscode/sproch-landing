import { LockedModule } from "@/components/admin/LockedModule";

export default function ContenidoPage() {
  return (
    <LockedModule
      title="Textos del sitio"
      description="Edición de los textos institucionales (inicio, nosotros, socios)."
    />
  );
}
